<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hfad.mygamelist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.TopLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RPGCategoryActivity"></activity>
        <activity android:name=".Activities.RPGDetailActivity"></activity>
        <activity android:name=".ActionCategoryActivity"></activity>
        <activity android:name=".Activities.ActionDetailActivity"></activity>
        <activity android:name=".ShooterCategoryActivity"></activity>
        <activity android:name=".Activities.ShooterDetailActivity"></activity>
        <activity android:name=".Activities.HelpActivity"></activity>
        <activity android:name=".Activities.FeedbackActivity"></activity>
        <activity android:name=".Activities.SearchActivity"></activity>
        <activity android:name=".MainUserGames"></activity>

    </application>

</manifest>